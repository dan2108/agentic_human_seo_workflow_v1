// Aftercare Dashboard - Day 7 / 30 / 90 performance metrics
// TODO: load aftercare reports for job, render tabbed dashboard with delta charts
export default function AftercarePage({ params }: { params: { jobId: string } }) {
  return <div>Aftercare {params.jobId}</div>;
}
