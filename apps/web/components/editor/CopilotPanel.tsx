"use client";
// CopilotPanel - right panel: streaming Claude API chat interface
// TODO: POST to /api/copilot SSE endpoint, stream response as suggestion bubbles
export default function CopilotPanel({ draftId }: { draftId: string }) {
  return <aside className="p-4 bg-gray-900">Copilot Panel</aside>;
}
