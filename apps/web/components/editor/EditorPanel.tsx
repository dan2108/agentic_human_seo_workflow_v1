"use client";
// EditorPanel - centre panel: TipTap rich text editor
// TODO: initialise TipTap with StarterKit + custom copilot extension
export default function EditorPanel({ draftId }: { draftId: string }) {
  return <main className="p-8 bg-gray-950">Editor Panel</main>;
}
