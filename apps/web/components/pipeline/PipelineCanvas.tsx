"use client";
// PipelineCanvas - React Flow diagram with live Supabase Realtime status
// TODO: subscribe to job_steps realtime channel, update node status on change
export default function PipelineCanvas({ jobId }: { jobId?: string }) {
  return <div className="w-full h-full bg-gray-950" />;
}
