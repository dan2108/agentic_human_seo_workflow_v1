// AuditGate - Gate 1: displays synthesis report + drag-and-drop priority reordering
// TODO: render findings list with react-beautiful-dnd, approve/reject with comment
export default function AuditGate({ gateId }: { gateId: string }) {
  return <div>Audit Gate {gateId}</div>;
}
