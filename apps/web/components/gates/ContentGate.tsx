// ContentGate - Gate 3: side-by-side content preview + QA report with annotations
// TODO: render TipTap readonly preview left, QA findings right, approve/revise controls
export default function ContentGate({ gateId }: { gateId: string }) {
  return <div>Content Gate {gateId}</div>;
}
